import assert from "node:assert/strict";
import test from "node:test";

test("the foundation test convention executes TypeScript tests", () => {
  assert.equal("alpamayo".toUpperCase(), "ALPAMAYO");
});
